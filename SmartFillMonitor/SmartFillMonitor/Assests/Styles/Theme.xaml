<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:SmartFillMonitor.ViewModels"
                    xmlns:views="clr-namespace:SmartFillMonitor.Views">
    <!--主应用深色背景-->
    <SolidColorBrush x:Key="AppBgDark" Color="#1E1E1E"/>
    <!--内容、面板背景-->
    <SolidColorBrush x:Key="AppBgPanel" Color="#252526"/>
    <!--Tap、Header背景-->
    <SolidColorBrush x:Key="AppBgHeader" Color="#2D2D30"/>
    <!--主按钮、选中导航、边框背景-->
    <SolidColorBrush x:Key="AppAccentColor" Color="#007ACC"/>
    <!--主要文字颜色：正文、标题背景-->
    <SolidColorBrush x:Key="AppTextMain" Color="#FFFFFF"/>
    <!--次要文字背景颜色、占位文本背景-->
    <SolidColorBrush x:Key="AppTextMuted" Color="#AAAAAA"/>
    <!--成功：显示成功、正常状态背景-->
    <SolidColorBrush x:Key="AppSuccess" Color="#4CAF50"/>
    <!--信息颜色：用于信息提示背景-->
    <SolidColorBrush x:Key="AppInfo" Color="#4CAF50"/>
    <!--警告颜色背景-->
    <SolidColorBrush x:Key="AppWarning" Color="#FF9800"/>
    <!--错误、危险颜色背景-->
    <SolidColorBrush x:Key="AppDanger" Color="#D50000"/>
    <!--通用边框颜色：控件边框、Card背景-->
    <SolidColorBrush x:Key="AppBorderBrush" Color="#3E3E42"/>
   
    <!--Navigation Item Style-->
    <Style x:Key="NavItemStyle" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource AppTextMuted}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="3,0,0,0"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="20,15"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#3E3E42"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="#37373D"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="{StaticResource AppAccentColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <DataTemplate DataType="{x:Type vm:DashBoardViewModel}">
        <views:DashBoardView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:DashQueryViewModel}">
        <views:DashQueryView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:LogsViewModel}">
        <views:LogsView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AlarmsViewModel}">
        <views:AlarmsView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:SettingViewModel}">
        <views:SettingView/>
    </DataTemplate>
    
    <!--TabControl Style-->
    <Style TargetType="TabControl" x:Key="AppTabControlStyle">
        <Setter Property="Background" Value="{StaticResource AppBgDark}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource AppTextMain}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0"
                                    Background="{StaticResource AppBgHeader}"
                                    BorderBrush="{StaticResource AppBorderBrush}"
                                    BorderThickness="0,0,0,1"
                                    Padding="6,8,0,0">
                                <TabPanel x:Name="HeaderPanel"
                                          Panel.ZIndex="1"
                                          IsItemsHost="True"
                                          KeyboardNavigation.TabIndex="1"/>
                            </Border>
                            <Border Grid.Row="1"
                                    Background="{StaticResource AppBgDark}"
                                    BorderThickness="0"
                                    Padding="4,0,4,4">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent"/>
                            </Border>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--TabItem Style-->
    <Style TargetType="TabItem" x:Key="AppTabItemStyle">
        <Setter Property="Foreground" Value="{StaticResource AppTextMuted}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="20,10"/>
        <Setter Property="Margin" Value="2,0,0,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="8,8,0,0"
                            Padding="{TemplateBinding Padding}"
                            Margin="{TemplateBinding Margin}">
                        <Grid>
                            <Border x:Name="TabAccentBar"
                                    Background="Transparent"
                                    CornerRadius="2"
                                    Height="3"
                                    Width="24"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Center"/>
                            <ContentPresenter x:Name="ContentSite"
                                              ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="0,3,0,0"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="#2F3237"/>
                            <Setter Property="Foreground" Value="{StaticResource AppTextMain}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource AppBgDark}"/>
                            <Setter TargetName="TabAccentBar" Property="Background" Value="{StaticResource AppAccentColor}"/>
                            <Setter TargetName="TabAccentBar" Property="Width" Value="32"/>
                            <Setter Property="Foreground" Value="{StaticResource AppTextMain}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--BorderStyle-->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AppBgDark}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="15"/>
    </Style>

    <!--Combox Style-->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="Black"/>
        <Setter Property="BorderBrush" Value="Black"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="8"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AppAccentColor}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>   